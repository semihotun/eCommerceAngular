import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ecommerceusernativeui',
  appName: 'ecommerceusernativeui',
  webDir: 'www',
};

export default config;
