import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ecommerceadminnativeui',
  appName: 'ecommerceadminnativeui',
  webDir: 'www',
};

export default config;
